---
const quests = [
  'Document the multi-framework architecture',
  'Highlight animation stack choices',
  'Map content pipeline for resume YAML',
  'Prep GitHub Pages deployments'
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Myfolio Astro Hub</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource-variable/inter/index.min.css" />
    <style>
      :root {
        color-scheme: dark;
        font-family: 'InterVariable', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: radial-gradient(circle at 20% 20%, #1f2937, #0b1021);
        color: #e2e8f0;
      }
      main {
        max-width: 960px;
        margin: 0 auto;
        padding: 3rem 1.5rem 4rem;
      }
      .card {
        background: rgba(17, 24, 39, 0.8);
        border: 1px solid #1f2937;
        border-radius: 1.25rem;
        padding: 1.5rem;
        box-shadow: 0 25px 70px rgba(0, 0, 0, 0.35);
      }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.45rem 0.9rem;
        border-radius: 999px;
        background: linear-gradient(120deg, #a5b4fc, #22d3ee);
        color: #0f172a;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <main>
      <p class="pill">Astro · Docs · Islands</p>
      <h1 style="font-size: clamp(2.5rem, 5vw, 3.5rem); margin: 1rem 0;">The Knowledge Base</h1>
      <p style="max-width: 60ch; line-height: 1.6; color: #cbd5f5;">
        This Astro space hosts roadmap notes, changelogs, and posts for the Myfolio build. It complements
        the SvelteKit experience with static-first content and opt-in islands for demos.
      </p>

      <section style="display: grid; gap: 1rem; margin-top: 2rem;">
        {quests.map((quest) => (
          <article class="card">
            <h2 style="margin: 0 0 0.5rem 0;">{quest}</h2>
            <p style="margin: 0; color: #94a3b8;">Tracking in Astro ensures deployable notes for each phase.</p>
          </article>
        ))}
      </section>
    </main>
  </body>
</html>
